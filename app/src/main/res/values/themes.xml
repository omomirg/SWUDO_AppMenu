<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GuruProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="android:textColor">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GuruProject" parent="Base.Theme.GuruProject">
        <!-- Add the following style for CalendarView -->
        <item name="android:dateTextAppearance">@style/CalendarDateTextAppearance</item>
    </style>

    <!-- New style for the CalendarView's date text appearance -->
    <style name="CalendarDateTextAppearance">
        <item name="android:textColor">@color/white</item>
        <!-- Add any other text styles you want to customize here -->
    </style>

</resources>

